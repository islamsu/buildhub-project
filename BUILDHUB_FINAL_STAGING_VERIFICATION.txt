================================================================================
BUILDHUB — STAGING VERIFICATION RECORD
================================================================================

Last updated : 2026-08-24
Target       : https://buildhub-staging.onrender.com

--------------------------------------------------------------------------------
WHY THIS FILE WAS REWRITTEN
--------------------------------------------------------------------------------

The previous contents of this file recorded a "FINAL LIVE STAGING VERIFICATION"
pinned to commit c77c409f4b08846854690329c4dd4d863e6a086e, reporting 174/174
checks passed with 8 skipped.

That record became MISLEADING rather than merely out of date, because it
asserted things that have since been disproven by execution:

  - it pinned `main` to c77c409, which has moved twice since;
  - it recorded "SKIP 23. a real administrator signs in and loads the admin
    surface", which now PASSES;
  - it stated that the rendered admin surface behind authentication was
    unproven, which is no longer true;
  - its tally of 174/174 with 8 skips has been superseded by a materially
    larger suite.

A SHA-pinned "final" report is stale the moment the SHA moves. Correcting it
rather than deleting it keeps the audit trail intact; leaving it as it stood
would have preserved evidence that reads as current and is not.

--------------------------------------------------------------------------------
WHAT IS TRUE AS OF THIS COMMIT
--------------------------------------------------------------------------------

The authoritative live-staging result is whatever the most recent Staging QA
run reports against the SHA that /version actually serves. It is not this file.
This file records where to look and what was established, not a frozen tally.

Established by execution, against live staging:

  - Deployment provenance is ASSERTED, not assumed. The gate is dispatched with
    expect_commit and fails on a mismatch; it never passes provenance by
    default.
  - The full administrator lifecycle is proven live: Super Admin sign-in,
    the administrators page, sub-admin invitation, redemption, single-use
    enforcement, all four specialist roles, permission boundaries, refusal of
    self-elevation, and deactivation.
  - The AUTHENTICATED admin browser surface is proven in real Chromium -
    section 26 - signing in through the form rather than calling the API.
  - The workflow fails when the harness fails. Proven in both directions:
    a deliberate negative control produced a red workflow, and clean runs
    produce green ones. `set -o pipefail` is pinned by a regression test.

Four skips are EXPECTED and are honest, not failures:

  1. password reset            - staging SMTP is not configured
  2. end-to-end S3 upload      - staging S3 is not configured
  3. product database columns  - the staging catalogue is intentionally empty
  4. category filtering        - the staging catalogue is intentionally empty

Skips are never counted as passes. The harness prints
"Skipped checks are NOT passes" at the end of every run.

--------------------------------------------------------------------------------
WHERE THE CURRENT EVIDENCE LIVES
--------------------------------------------------------------------------------

  Staging QA workflow : .github/workflows/staging-qa.yml
  The harness itself  : scripts/staging-qa.mjs
  Coverage guarantees : server/stagingGate.test.ts
  Run history         : GitHub Actions -> Staging QA

To reproduce the current position, dispatch Staging QA with base_url set to the
staging URL and expect_commit set to the exact 40-character SHA that
/version reports. Read the tally AND the workflow conclusion; a green tick alone
is not evidence, and neither is a tally without a named commit.

--------------------------------------------------------------------------------
PRODUCTION
--------------------------------------------------------------------------------

Production was not touched by any work recorded here. Production remains NO-GO
on infrastructure grounds - domain, database, host, verified backups with a
tested restore, S3 and SMTP - none of which is a code defect.

================================================================================
